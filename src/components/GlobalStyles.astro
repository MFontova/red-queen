<style is:global>
  /* Tipografía */

html::-webkit-scrollbar {
    display: none;
}

body {
    font-family: 'Raleway Variable', sans-serif !important;
    background-color: #1b1b1b;
    color: #f5f5f5;
    margin: 0;
    padding: 0;
}

/* Contenedor principal */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Encabezados */
h1, h2, h3 {
    @apply text-orange-700;
    text-align: center;
}

h1 {
    font-size: 3em;
    margin-bottom: 20px;
}

h2 {
    font-size: 2em;
    margin-bottom: 15px;
}

h3 {
    font-size: 1.5em;
    margin-bottom: 10px;
}

/* Botones */
button {
    @apply text-orange-50 bg-orange-700 px-3 py-2 rounded-md;
    /* background-color: #ff4c4c;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease; */
}

input[type=submit] {
    @apply text-orange-50 bg-orange-700 px-3 py-2 rounded-md;
    font-family: 'Merriweather'
}

/* Enlaces */
a {
    @apply text-orange-50 bg-orange-700 px-3 py-2 rounded-md;
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

/* Puzle contenedor */
.puzzle-container {
    background-color: #2a2a2a;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    margin-bottom: 20px;
}

/* Inputs y cajas de texto */
input, textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    background-color: #3a3a3a;
    color: #fff;
}

input:focus, textarea:focus {
    outline: none;
    border-color: #ff4c4c;
}

/* Imágenes y objetos ocultos */
.hidden-object {
    width: 50px;
    height: 50px;
    margin: 5px;
    cursor: pointer;
    transition: transform 0.3s ease;
}

.hidden-object:hover {
    transform: scale(1.2);
}

/* Layout de navegación */
.navbar {
    background-color: #2a2a2a;
    padding: 10px;
    text-align: center;
}

.navbar a {
    margin: 0 15px;
    color: #fff;
    font-weight: bold;
}

.navbar a:hover {
    color: #ff4c4c;
}

/* Pie de página */
footer {
    background-color: #1b1b1b;
    color: #777;
    text-align: center;
    padding: 20px 0;
    position: fixed;
    width: 100%;
    bottom: 0;
}

</style>